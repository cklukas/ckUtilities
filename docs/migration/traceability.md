# ckVision migration traceability ledger

This is the living WP-0 record linking each legacy product area to a
framework-neutral acceptance suite and its migration work package. It records
what users must be able to do, not how Turbo Vision implemented it.

## Application inventory

| ID | Product area | Core acceptance journeys | Owner WP | Status |
|---|---|---|---|---|
| CKU-SHELL | shared shell | command invocation, focus, menu/status/help, theme, resize, quit | WP-2 | native shell implemented; headless acceptance covers every suite theme, Mono16 color degradation, wide/80x24/narrow sizes, below-floor handling, recovery, launcher return, and standard quit |
| CKU-JSON | `ck-json-view` | open/reload/close, navigate, expand level, find, copy | WP-3 | native pilot implemented; its headless scenario covers filesystem load, copy, malformed-reload preservation, 64-level UTF-8 search, narrow resize, keyboard/mouse selection, and a 2,048-entry search with a visible-frame composition cap. Its native Reload command keeps the existing provider and a surviving selected node stable, while rejecting a malformed replacement without discarding the last valid snapshot. A 12,000-entry snapshot then refreshes to two indexed nodes under the same visible-frame cap, proving that obsolete application provider indexes are released. The application exposes its domain nodes through ckVision's stable-ID `TreeModel`; a one-million-root library test proves only five items are queried to paint five visible rows. |
| CKU-LAUNCH | `ck-utilities` | launch tool, multi-window utilities, calculator, ASCII, color, event diagnostics | WP-4 | native launcher conversion implemented: calendar, ASCII table, calculator, color selector, and bounded application diagnostics all run as native Desktop surfaces. Headless coverage includes repeated windows, standard close/quit lifecycle, keyboard/mouse list selection, and resize. The staged-product verifier exercises `ck-utilities --launch <child> --help` for every converted child, confirms exact child exit-status propagation, and separately confirms a missing child fails before execution. |
| CKU-FIND | `ck-find` | compose, validate, persist, preview, and execute a search specification | WP-5 | native guided form, JSON-backed saved-search adapter, joinable injected execution service, UI-thread completion, cancellation, and explicit Yes/No/Cancel-confirmed deletion of matching regular files or symbolic links implemented. A late execution completion checks its lifetime gate before dereferencing the destroyed presentation. Directory deletion and custom command execution remain withheld; the latter needs a separately sandboxed policy |
| CKU-DU | `ck-du` | scan, navigate, sort, inspect, cancel, and execute cloud actions | WP-6 | native stable-ID `TreeModel`/Table, injected joinable directory scanning, file-list Table, progress, UI-thread delivery, and cancellation implemented. Rescan keeps the last valid snapshot visible until a replacement succeeds; a surviving path retains its selection and expanded state, while cancelled/invalid replacements retain the prior view. A 2,048-entry snapshot then refreshes to four indexed nodes under a visible-frame composition cap, proving stale provider indexes are released. Deterministic manual-service acceptance proves that scan progress/completion, file-list completion, and cloud progress/completion arriving after teardown are ignored before the destroyed presentation is touched; the provider-model lifetime case is also covered under ASan/UBSan. The selected-directory cloud boundary now offers a cancellable macOS Foundation iCloud download request and an explicit-confirmation local-copy eviction request; request acceptance, failure/retry, and unsupported-platform behavior are surfaced honestly. Recursive/provider policy remains |
| CKU-CONFIG | `ck-config` | inspect/edit/reset/import/export configuration and key bindings | WP-7 | native injected-registry inspector/editor with typed edit/reset and injected JSON-default save/reload/import/export implemented. Option profiles are flat and version-marked while existing unmarked profiles remain readable; known values are validated before committing any changes and unknown future values survive a load/edit/save round trip. Option metadata can mark a derived setting read-only, keeping its value visible but disabling native edit/reset execution. Import is transactional at the application boundary: a failed persistence policy restores the exact prior override snapshot. Its shortcut table now captures typed normalized chords, reports occupied bindings before an explicit replacement, and persists by stable command key. It selects separate built-in-default and personal-binding schemes; selecting defaults leaves personal bindings intact and the next edit reactivates the personal scheme. A scheme switch is likewise transactional: if its bindings cannot reload, Config restores the previously persisted scheme. One suite catalog drives both each executable's declared title/category/default chord and Config's command-only registries, so app-specific bindings are edited and stored under the target app while shared `ckv.*` bindings remain global; target executables reload both at startup. Final configuration acceptance remains |
| CKU-EDIT | `ck-edit` | edit, format, search, save, resolve conflict, and close Markdown documents | WP-8 | native injected-file editor with open/save/save-as, explicit dirty Save/Discard/Cancel close, application-owned Markdown syntax profile, and conservative whitespace normalization committed as one revision-bound document transaction. It preserves fenced/indented code and intentional hard breaks. The native Edit menu exposes document-wide undo/redo and a viewport-only word-wrap toggle, leaving source bytes and logical positions unchanged. The framework-independent Markdown core now toggles bold/italic/strikethrough/delimiter-safe inline-code selection, heading 1–6 markers, task-list state, block-quote level, basic bullet/ordered-list formatting, inline links/images, footnotes, nested-list indentation, smart-list continuation, and conservative paragraph reflow while protecting code; task commands promote ordinary text, preserve ordinary list marker/indentation, and deliberately leave headings, quotes, and table-looking lines unchanged, while quote commands preserve blank-line continuity and nest mixed lines by one level. List commands sequentially number ordered selections, preserve task markers while converting styles, and remove task markers when de-listing. Indent/outdent moves selected list markers by two spaces, preserves blank separators and newline style, keeps root lines stationary on outdent, and recognizes deeper markers only with contiguous parent-list context, so indented code is untouched. A list item's terminal Enter preserves its bullet marker/indentation, advances ordered markers, restarts task items unchecked, and exits an empty item without trailing whitespace. Link/image insertion uses a native destination dialog, supports parenthesized destinations, and removes a complete link/image or selected label/alt text. The footnote dialog validates a unique identifier, then commits both the reference and a blank-line-separated definition in one document transaction and places the cursor in the definition body. Native literal find, find-next, replace, and atomic replace-all dialogs retain case and whole-word choices, while unchanged queries replace the active selected match. Reflow uses 80 UTF-8 code points, retains first-line indentation and newline style, and leaves code, hard breaks, headings, quotes, lists, tables, references, and Setext headings byte-for-byte unchanged. The controller restores Unicode selections through the validated ckVision API. Headless acceptance covers Unicode selection, safe mixed-newline normalization, malformed UTF-8 rejection without replacing an open document, and a 2,048-line Unicode document whose logical cursor and source text survive word-wrap and resize. A save conflict preserves the external file and requires an explicit Save As, reload/discard, or continue-editing choice; workflow-depth acceptance remains |
| CKU-CHAT | `ck-chat` | edit/send/cancel prompts, stream/copy/export responses, manage models/prompts | WP-9 | native FlowView transcript, prompt dialog, injected streaming response service, cancellation, worker adapter, UI-thread lifetime guard, shared Markdown-to-FlowView adaptation, and injected transcript export implemented. Response chunks/completion and model-download progress/completion check the lifetime gate before dereferencing a torn-down presentation. Prompt selection/create/edit/default restoration/confirmed custom deletion and model selection/deactivation/confirmed local deletion use framework-neutral adapters over `ckai_core`, with active prompt/model IDs and completed prior turns propagated into response requests. The same boundary offers catalog-backed background download, cancellation, cached UI reads, rate-limited posted progress/completion, and worker-owned active-model loading plus cancellable chunk generation. The live view displays the latest 160 messages only, with a retention notice, while complete history remains available to export and the response service; first response output is immediate and later small chunks are coalesced through ckVision's checked active-block replacement with incremental realized-tail reflow. Real-model runtime evidence remains |
| CKU-RELEASE | packaging | build, test, install, package, and run every executable | WP-10 | installed-product gate rebuilds the suite, stages a disposable prefix, runs `--help` for every native executable, and exercises every converted child through the installed launcher. `CKTOOLS_CKVISION_CUTOVER=ON` now composes only ckVision products and framework-neutral cores under the production binary names, without configuring or installing the legacy UI runtime; `verify_ckvision_cutover` rejects legacy linkage, installed artifacts, and installed public-header references. Upstream candidate acceptance, permanent legacy removal, and the full release matrix remain |

Every row will gain links to domain tests, deterministic headless scenarios,
interactive platform tests, and release evidence before its work package can
close.

## Initial ckVision gap ledger

| Gap ID | Neutral requirement | First validating slice | State |
|---|---|---|---|
| CV-GAP-001 | A result controller needs to reveal and select a stable node without synthesizing input. Large changing hierarchies need provider-backed incremental refresh without duplicating their domain tree. | CKU-JSON, CKU-DU | `TreeView::reveal_and_select` and stable-ID `TreeModel` are in clean candidate `9f097ecde219a35971ccde6f8732b07ef021c35b`; TreeView owns expansion/selection and queries only visible paths. JSON and disk usage consume it directly, and a one-million-root test proves five painted rows issue five item queries. JSON’s native Reload keeps a surviving selection and provider instance stable, rejects malformed replacement transactionally, and drops a 12,000-entry index to two current nodes. Disk usage retains its last valid scan during rescan and carries surviving selection/expansion into the replacement while dropping a 2,048-entry index to four nodes. No further generic API gap is evidenced. |
| CV-GAP-002 | A long streaming rich-text document may need active-block updates, bottom anchoring, selection/copy, and bounded relayout. | CKU-CHAT | checked `FlowView::replace_block` with incremental realized-tail reflow implemented in ckVision candidate `bf4c1c6`; ckUtilities bounds the live transcript and coalesces small response updates. Viewport anchoring and selection/copy require an independently specified generic need |
| CV-GAP-003 | Runtime keymap editing needs a focused control that captures a normalized chord without allowing that chord to dispatch as a command. Conflict diagnostics and persistence remain application policy. | CKU-CONFIG | `KeyChordCapture` implemented in ckVision candidate `0bef919`; ckUtilities owns stable-key persistence, conflict confirmation, and shared-binding reload |
| CV-GAP-004 | The initial color-selection workflow uses ckVision's existing declarative radio-form dialog; bounded application diagnostics use its injected diagnostics observer. No framework enhancement is currently justified. | CKU-LAUNCH | resolved for the initial launcher slice; reassess only if a reusable palette control has independent adopters |
| CV-GAP-005 | Declarative forms need a labeled multi-line text field that retains ordinary Memo editing, returns a complete text value, and receives accept-time validation/error styling. | CKU-CHAT | `FieldKind::Memo`, `memo_rows`, typed result wiring, and dedicated Memo invalid roles implemented in ckVision candidate `351592d` |
| CV-GAP-006 | A controller that ends before its borrowed Application needs to remove installed chrome and command callbacks without retaining stale views or callable controller pointers. | CKU-JSON, CKU-DU | `ApplicationShell::detach_desktop` and its liveness-guarded standard Help handler are in candidate `9f097ec`; `SuiteShell` detaches chrome and withdraws its own commands, while every native controller owns its `this`-capturing registrations through `SuiteCommandScope`. JSON and disk lifetime scenarios render another frame after destruction, Find proves a saved controller command is inert after destruction, and the controller suites pass under ASan/UBSan |
| CV-GAP-007 | A controller that commits a semantic document transformation needs to restore a current selection without private editor access or stale-revision cursor mutation. | CKU-EDIT | `TextEditor::set_selection` is in clean candidate `9f097ecde219a35971ccde6f8732b07ef021c35b`; it validates current revision, document positions, and grapheme alignment before moving the selection. ck-edit uses it after one Markdown transaction, with direct ckVision and adopting-application normal/ASan/UBSan coverage |
| CV-GAP-008 | A language-aware editor action needs to intercept Enter without copying the editor widget or treating pasted newline text as a key event. | CKU-EDIT | `TextEditor::set_newline_handler` is in clean candidate `c25a324aa1db9c24851085a6ff84f5ae47e9014b`; it lets the application handle a key by committing its own revision-safe document transaction or return to normal newline insertion. ck-edit uses the public seam for Markdown smart-list continuation, with normal/ASan/UBSan library and application coverage |

An entry becomes a ckVision implementation only after it passes the promotion
test in [the migration roadmap](../../tv_to_ckvision.md#82-promotion-test).
